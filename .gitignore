# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code: skills are shared, but local config and run artifacts are not.
# settings.json holds machine-specific absolute paths and personal tool approvals.
.claude/settings.json
.claude/settings.local.json
.claude/skills/*/out/
# Maintainer-only: documents pushing straight to main + this machine's dual gh accounts,
# which contradicts the branch-and-PR flow in CONTRIBUTING.md. Delete this line to share it.
.claude/skills/push/

# Downloaded verification bundles (run outputs, not source)
scripts/retirement-verification-*.json

# Python artifacts (from scripts/verify_plan.py)
__pycache__/
*.pyc

*.pdf