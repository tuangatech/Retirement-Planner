{
    "name": "verify-ui",
    "private": true,
    "type": "module",
    "description": "Playwright harness for the verify-ui skill. Deliberately separate from the project's package.json so Playwright never becomes a project dependency.",
    "dependencies": {
        "playwright": "^1.50.0"
    }
}
